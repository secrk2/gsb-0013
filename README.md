# 报关通 · 报关行自研报关作业与合规风控平台

进出口报关单**全生命周期**管理 + 合规风控骨架。本期落地「**报关作战台**」与「**委托与客户**」两条主线。

- 后端：Python 3.11 + FastAPI + SQLAlchemy + SQLite，监听 **7106**
- 前端：零构建原生 SPA（HTML/CSS/JS），由 Nginx 在 **8106** 托管并反代 `/api → 7106`
- 一键拉起：`docker compose up -d --build`，打开 http://localhost:8106

---

## 一键启动

```bash
docker compose up -d --build
# 前端/入口：http://localhost:8106
# 后端直连：http://localhost:7106/api/health
```

首次启动后端会**自动建表并预置演示数据**（SQLite 存于命名卷 `bgt-data:/data`）。
重置演示数据：

```bash
docker compose down -v && docker compose up -d --build
```

> ⚠️ 本期新增了监管场站/车位/查验员/排期留痕表与查验字段，**升级旧数据卷必须先 `down -v` 重置**
> （自动建表只补缺失的表，不会给旧表加列）；首次使用不受影响。

## 预置账号（统一口令 `bgt123456`）

| 账号 | 角色 | 说明 |
| --- | --- | --- |
| `zhang_wei` | 报关员 | 张伟，盐田/蛇口口岸，可立项派单、录入、离线作业、二次确认看全名 |
| `li_na` | 报关员 | 李娜，蛇口/南沙口岸 |
| `wang_hua` | 海关审单员 | 王华，审单、布控查验、放行、结关、排查验计划、改派 |
| `zhou_min` / `wu_tao` / `zheng_kai` | 查验员 | 周敏（冷链）/ 吴涛（冷链+危化）/ 郑凯（大型+冷链） |
| `he_jun` | 查验员 | 何军（冷链，预置为**请假中**，演示人请假改派） |
| `admin_huateng` | 企业管理员 | 陈志成，华腾企业（仅能看本企业单据） |
| `admin_yuanhang` / `admin_xianchi` | 企业管理员 | 远航 / 鲜驰企业管理员 |
| `zhao_jing` | 监管员 | 赵静，备案审核、留痕审计、全局态势、排期统筹 |

预置数据：**4 家企业**（3 家已备案 + 1 家备案中）、生效/待签委托合同、**17 张主线报关单**覆盖
`委托中 / 已录入 / 审单中 / 查验中 / 已放行 / 已结关 / 已撤销`，含 1 张逾期税款、1 张 3 日内到期税款；
另有 **26 张近 3 个月历史报关单/查验**用于及时率统计。
查验侧预置 **3 个监管场站 10 个车位**（盐田 A-12 故障、南沙 NS-02 故障）、本周查验甘特（逾期/故障车位/请假查验员/
背靠背待顺延链）、南沙场站「排期全取消」、0018 单「无有效排期」，以及近 3 个月历史查验：
**8 月频繁改派 22 次 → 完成率 100% 但查验准点率仅 30%**（双口径背离演示）。

---

## 功能与验收点对照

### 1. 委托到客户做成链路，非法回退拦下说原因
链路强约束（见 `app/deps.py::ensure_active_contract`、`app/routers/clients.py`）：

```
企业备案(filing) ──审核通过──> 已备案(active) ──签署──> 委托合同生效(active) ──> 报关单立项(entrusted)
```

- 给**备案中/被驳回**企业签合同 → 409，返回具体原因（含驳回原因）。
- 凭**未生效/他企业**合同立项 → 409，说明必须先签生效合同。
- 状态机集中在 `app/models.py::guard_transition`：终态不可变更、不允许跳态/回退，
  非法时返回形如「不允许从 X 直接变为 Y，当前允许的下一步是…」的可读原因。
  唯一合规回退通道 `审单中 → 已录入（退单补录）`、`查验中 → 审单中（查验异常重审）`已显式放行。

### 2. 企业名脱敏 + 二次确认 + 留痕
- 默认展示名 = 字号缩写 + 编号（如「华腾 E0101」），统一社会信用代码同步掩码。
- 报关员查看全名必须**二次确认并填写 ≥10 字理由**，写入 `name_reveals` 表。
- 「全名查看留痕」页（监管员看全部、报关员看自己的）可审计追溯。

### 3. 报关作战台
- 全行六态待审漏斗 + 按企业聚合的迷你漏斗/待审 KPI。
- 查验排期表（今日数、逾期红点）。
- 征税红点：**逾期未缴（红）**、**3 日内到期（黄）**，可一键跳单据处置。
- 响应式适配 **1440 桌面 / 1024 平板 / 390 手机**（断点 1200px、720px，侧栏在窄屏转横向标签）。

### 4. 企业间隔离，越权给错误态而非空白页
- 企业管理员只能读本企业数据；列表带他企业 `enterprise_id` → 403；
  直接打开他企业报关单详情 → **明确的错误态页**（错误码 + 原因 + 返回入口），不是空白页。
- 越权执行非本角色操作同样被状态机角色表拦下（403 + 原因）。

### 5. 口岸离线：不能拿旧状态糊弄，恢复后合并且幂等
- 断网时顶部红色横幅常驻；所有缓存视图打「🕓 离线缓存 · 截至 …」水印，
  并明示**非实时状态**、不提供误导性的操作按钮。
- 断网可：①本地起草报关单（带稳定 `idem_key` + `client_ref`）②本地排队流转（带 `client_event_id`）。
- 恢复联网后自动补传：
  - 建单走 `Idempotency-Key` 与 `client_ref` 双保险，**绝不产生重复报关单**，重放返回首次结果；
  - 流转走 `/declarations/{id}/sync-offline`，在服务端逐条重放状态机：
    已应用→跳过（幂等）、非法/构成回退→进**冲突清单**弹给人工，绝不静默覆盖。
- 在线流转带 `expected_version` 乐观锁：版本过期返回 409 `version_conflict`，
  前端拉最新快照后弹出「合并确认」，基于新版本重提。

> 浏览器里演示断网：DevTools → Network → Offline，或直接拔网；恢复后会自动触发合并。

### 6. 审单布控 → 监管场站查验排期（周日甘特 · 改派链式顺延）

海关审单员**逻辑审核通过**后在报关单详情「安排查验」，系统校验货/车位/查验员三方资质并把单据转入「查验中」；
查验在**监管场站**排期，车位与持证查验员均为有限资源。代码核心在 `app/scheduling.py`（单一事实来源）+
`app/routers/scheduling.py`。

- **周 / 日双甘特**：行=车位 + 查验员双维度，任务块在两维各挂一份；营业窗口外、周末用斜纹底标出，
  红色当前时刻线。可按场站切换、前后翻周/日。
- **拖拽落位**：拖到车位行换时间/车位，拖到查验员行换时间/查验员，30 分钟吸附。
  落点**超营业窗口**（含闭场后、非作业日）→ 不静默挪动，弹**二次确认并强制填 ≥4 字原因**，
  以「超窗口落位」类型写入 `schedule_changes` 留痕。
- **冲突逐条标红、可点开看原因**（不是一句笼统报错）：阻断级返回结构化 `conflicts[]`，
  每条含 `code/title/reason/conflict_with`（对方单号、时段、车位、查验员）：
  - `bay_double_book` 同车位同段两单占；
  - `inspector_double_book` 同查验员同时两单（跨场站也查，人不能分身）；
  - `bay_qual_mismatch` / `inspector_qual_mismatch` 资质不匹配
    （冷链 HS02/03/16、危化 HS28/29、大型设备；货-台位类型-查验员资质三方比对，按 HS 章节+货名推导）；
  - `bay_broken` 车位故障、`inspector_leave` 查验员请假、`scheduled_in_past` 排在过去。
  甘特上命中的块红框脉冲，点开逐条展开；甘特加载即对存量排期做体检（故障/请假后旧排期自动翻红）。
- **改派（车故障 / 人请假）+ 链式重排**：从当前单解绑重派到新车位/查验员/时间，
  **该场站后续按资源线排的单按计划查验时间链式顺延**。算法为按原时间排序的**单遍最早可行槽**列表调度：
  每条只避让「改派目标单 + 锁定屏障 + 已就位的更早任务」，**只后移不前移**、传播方向严格向后，
  数学上不可能出现环形依赖；**已放行 / 已结关 / 已撤销 / 已完成查验的单一律不动**，
  链条撞上锁定屏障且 30 天内绕不开时明确报 `chain_blocked_by_locked` 交人工，绝不强推。
  先 **dry-run 预演**（本单变化、每张顺延单的原/新时间与顺延原因、锁定屏障清单）再确认；
  同一次连锁共用 `chain_batch` 批次号，逐单写「改派/链式顺延」留痕。
- **三种空态/错误态严格分开，不统一写「暂无数据」**：
  ① 加载失败/离线（错误码+原因+重试，不用旧缓存冒充）；
  ② 该单/该时段确实无排期（引导去安排）；
  ③ 排期全部取消（列出每条取消记录与原因，`all_cancelled` 标记）。
  报关单详情查验卡同样区分「尚无排期 / 排期全取消（含逐条取消原因）/ 有当前排期」。
- **查验及时率：口径由产品定，两口径并列且把口径写在界面上**（`/scheduling/timeliness`）：
  - **口径一 · 查验准点率（按日）**＝实际查验完成日 ≤ 应查验日的单数 ÷ 当月应查验单数。
    应查验日在布控排期时确定，**改派只动计划日、不回改应查验日**，未完成/逾期完成均不计入。
  - **口径二 · 查验完成率（按单量）**＝当月已完成查验单数 ÷ 当月派单数，不看比计划晚几天。
  - 界面同时给出两口径的公式/分子/分母/回答含义与背离说明：**频繁改派的月份两口径会相反**
    （预置 8 月：单最终都查完 → 完成率 100%，但实际查验日普遍晚于应查验日 → 准点率仅 30%），
    附近 6 个月两口径 + 改派次数趋势，背离月份高亮。

## 目录结构

```
.
├── docker-compose.yml          # 一键拉起 backend(7106)+frontend(8106)
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── app/
│       ├── main.py             # FastAPI 入口、统一错误格式
│       ├── models.py           # ORM + 状态机（guard_transition 单一事实来源）+ 查验资质推导
│       ├── scheduling.py       # 排期引擎：营业窗口/结构化冲突检测/改派链式重排（无环、锁终态）
│       ├── deps.py             # 登录态、企业间隔离、委托链路校验
│       ├── security.py         # pbkdf2 口令哈希、会话 token
│       ├── schemas.py          # 入参/序列化
│       ├── seed.py             # 预置数据（场站/车位/查验员/跨月排期与改派历史）
│       └── routers/ (auth / clients / declarations / dashboard / scheduling)
└── frontend/
    ├── Dockerfile
    ├── nginx.conf              # 8106 静态托管 + /api 反代 7106
    ├── index.html
    └── css / js (api、offline、scheduling、app)
```

## 本地开发（不用 Docker）

```bash
cd backend
python3 -m venv .venv && . .venv/bin/activate
pip install -r requirements.txt
BGT_DATA_DIR=./data uvicorn app.main:app --reload --port 7106
# 前端可用任意静态服务器，把 /api 代理到 127.0.0.1:7106
```

## 后续骨架可扩展点

电子缴款回执对接、HS 归类/价格合规风控规则引擎、证件与许可证校验、单一窗口报文对接、
查验异常处置工作流、企业信用分级看板。
